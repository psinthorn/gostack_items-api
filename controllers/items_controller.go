package controllers

import "net/http"

func Create(w http.ResponseWriter, r *http.Request) {
	// Authenthication

	// Received item from user request

	// Create item by call Create() method at services

	// Return item as JSON to user with http status 201

	return
}

func Get(w http.ResponseWriter, r *http.Request) {
	// Receive request

	// Get item by call Get() mehtod at services
	return
}
